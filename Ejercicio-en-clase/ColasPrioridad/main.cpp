#include <iostream>
#include <deque>
#include <algorithm>

#include "priorityQueue.h"

int main(){
    priorityQueue colaPrio;
    vacia = colaPrio.empt();
    cout << "La cola esta vacia? " << vacia << endl;
    colaPrio.enQueue(8);
    colaPrio.enQueue(1);
    colaPrio.enQueue(9);
    colaPrio.enQueue(3);
    colaPrio.enQueue(5);
    for(int i = 1; i<colaPrio.l.size(); i++)
        cout << l[i] << endl;
    primero = colaPrio.fron()
    cout << primero << endl;
    colaPrio.deQu();
    for(int i = 1; i<colaPrio.l.size(); i++)
        cout << l[i] << endl;

}
